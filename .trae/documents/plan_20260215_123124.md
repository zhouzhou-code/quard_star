根据您的指示，我将把注释优化工作的范围扩大到 `portmacro.h` 和 `port.c` 两个核心文件。

### 1. 目标文件
1.  `/home/zms/quard_star/trusted_domain/FreeRTOS-Kernel-v10.4.3/portable/GCC/RISC-V-S/portmacro.h` (头文件)
2.  `/home/zms/quard_star/trusted_domain/FreeRTOS-Kernel-v10.4.3/portable/GCC/RISC-V-S/port.c` (实现文件)

### 2. 修改内容与技术依据

我将使用 Doxygen 格式 (`/** @brief ... */`) 对这两个文件中的所有重要宏、变量、类型和函数进行详细注释。

#### A. `portmacro.h` (头文件)
*   **RISC-V 架构字长 (`__riscv_xlen`)**:
    *   **注释重点**: 解释其来源（编译器预定义）、含义（XLEN）、与 RV32/RV64 的对应关系，以及在 FreeRTOS 中决定 `portSTACK_TYPE` 的作用。
    *   **依据**: *RISC-V C API Specification*。
*   **栈增长方向 (`portSTACK_GROWTH`)**:
    *   **注释重点**: 明确 `-1` 代表向下增长，这是 RISC-V ABI 标准，直接影响 `pxTopOfStack` 的计算。
    *   **依据**: *RISC-V ELF psABI Specification* (Stack Layout)。
*   **Tick 周期 (`portTICK_PERIOD_MS`)**:
    *   **注释重点**: 解释物理含义（毫秒/Tick），用于时间转换，提示 `configTICK_RATE_HZ` 的限制。
*   **内存对齐 (`portBYTE_ALIGNMENT`)**:
    *   **注释重点**: 解释 `16` 字节对齐是 RISC-V ABI 强制要求（支持 `long double`/`__int128`），影响栈和堆分配。
    *   **依据**: *RISC-V ELF psABI Specification* (SP Alignment)。
*   **临界区管理宏 (`portENTER_CRITICAL`/`portEXIT_CRITICAL`)**:
    *   **注释重点**: 解释其如何通过操作 `sstatus.SIE` 位来屏蔽/使能中断，以及如何维护 `xCriticalNesting` 计数。

#### B. `port.c` (实现文件)
*   **全局变量**:
    *   `xCriticalNesting` (**关键变量**):
        *   **注释重点**: 解释其用于跟踪临界区嵌套深度。在 S 模式下，由于不能简单地通过读取 `sstatus` 来判断中断嵌套状态（因为中断可能被屏蔽），必须使用此变量。初始值通常非零以防止在调度器启动前发生上下文切换。
*   **核心函数**:
    *   `pxPortInitialiseStack`:
        *   **注释重点**: 详细描述 RISC-V S 模式下的栈帧结构（31/32 个寄存器），解释 `sepc` 和 `sstatus` (SPP/SPIE) 的初始化要求。
    *   `xPortStartScheduler`:
        *   **注释重点**: 描述启动流程：配置 Tick 定时器 (SBI)、设置异常入口 (`stvec`)、启用中断 (`sie`)、启动第一个任务。
    *   `vPortEndScheduler`:
        *   **注释重点**: 说明其在嵌入式系统中的不可达性。

### 3. 执行步骤
1.  **`portmacro.h`**: 使用 `SearchReplace` 替换旧的简略注释，插入详细的 Doxygen 注释块。
2.  **`port.c`**: 使用 `SearchReplace` 为 `xCriticalNesting` 和关键函数添加/更新 Doxygen 注释。
3.  **验证**: 确保注释格式统一，术语准确。
4.  **报告**: 提交对比报告，列出修改点和技术依据。

请确认是否执行此计划。